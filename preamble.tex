% indent paragraphs and space between paragraphs
\setlength{\parindent}{7ex}
\setlength{\parskip}{0.2em}

% to indent by default the first paragraph of a section or subsection
\usepackage{indentfirst}
